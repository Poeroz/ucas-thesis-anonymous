\usetikzlibrary{arrows.meta, fit, backgrounds, positioning, calc}
\begin{figure}[htbp]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
    >=Stealth,
    % 顶部标题
    title/.style={draw, rounded corners=5pt, minimum height=1.2cm,
        align=center, font=\large\bfseries, inner sep=8pt, thick,
        fill=violet!10, draw=violet!70!black},
    % 科学问题
    prob/.style={draw, rounded corners=4pt, minimum width=3.8cm, minimum height=1.3cm,
        align=center, font=\small, inner sep=5pt, thick,
        fill=blue!10, draw=blue!70!black},
    % 研究工作
    work/.style={draw, rounded corners=3pt, minimum width=8.2cm, minimum height=1.0cm,
        align=center, font=\small, inner sep=5pt, thick,
        fill=teal!10, draw=teal!70!black},
    % 箭头
    arr/.style={->, thick, draw=black!35},
    prog/.style={->, very thick, draw=black!25, dashed},
    % 箭头标签
    arrlbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt},
]

% 列位置
\def\probX{0}
\def\workX{8.5}

% ===== 顶部标题（横跨两列） =====
\node[title] (top) at (4.2, 1.8) {端到端语音到语音生成\quad{\normalsize\normalfont 目标：高质量、低延迟}};

% ===== 科学问题列（从上到下） =====
\node[prob] (p1) at (\probX, 0) {\textbf{科学问题一}\\预训练模型的组合与利用};
\node[prob] (p2) at (\probX, -2.4) {\textbf{科学问题二}\\建模能力与解码效率的矛盾};
\node[prob] (p3) at (\probX, -5.4) {\textbf{科学问题三}\\多模态序列的同步流式生成};
\node[prob] (p4) at (\probX, -8.4) {\textbf{科学问题四}\\双向语音流的统一序列建模};

% ===== 标题到第一个问题 =====
\draw[arr] (top.south) -- (p1.north);

% ===== 科学问题递进箭头（带标签） =====
\draw[prog] (p1.south) -- node[arrlbl, right=2pt] {解码加速} (p2.north);
\draw[prog] (p2.south) -- node[arrlbl, right=2pt] {同步生成} node[arrlbl, left=2pt] {大模型} (p3.north);
\draw[prog] (p3.south) -- node[arrlbl, right=2pt] {全双工} (p4.north);

% ===== 研究工作列 =====
\node[work] (w1) at (\workX, 0) {基于模块化组合的端到端语音到语音翻译模型（第三章）};
\node[work] (w2) at (\workX, -2.4) {基于有向无环图的语音到语音翻译模型（第四章）};
\node[work] (w3) at (\workX, -4.8) {基于非自回归流式解码的实时语音对话大模型（第五章）};
\node[work] (w4) at (\workX, -6.0) {基于自回归流式解码的高质量语音对话大模型（第六章）};
\node[work] (w5) at (\workX, -8.4) {基于多信道交织的全双工语音对话模型（第七章）};

% ===== 科学问题到工作的连线 =====
\draw[arr] (p1.east) -- (w1.west);
\draw[arr] (p2.east) -- (w2.west);
\draw[arr] (p3.east) -- (w3.west);
\draw[arr] (p3.east) -- (w4.west);
\draw[arr] (p4.east) -- (w5.west);

\end{tikzpicture}%
}
\bicaption{\enspace 本文研究内容概览}{\enspace Overview of the research content}
\label{fig:research_overview}
\end{figure}
