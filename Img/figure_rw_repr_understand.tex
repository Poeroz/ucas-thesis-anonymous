\begin{figure}[!htbp]
    \centering
    \begin{tikzpicture}[
        box/.style={draw, rounded corners=3pt, minimum height=0.75cm, align=center, font=\small},
        databox/.style={draw, rounded corners=3pt, minimum height=0.75cm, align=center, fill=black!8, font=\small},
        featbox/.style={draw, rounded corners=3pt, minimum height=0.75cm, align=center, fill=black!5, font=\small, dashed},
        arr/.style={->, >=stealth, thick},
        lbl/.style={font=\footnotesize\bfseries},
    ]

    % 原始输入
    \node[databox, minimum width=1.6cm] (wave) at (0, 0) {语音波形};

    % 路径1：传统端到端
    \node[box, minimum width=1.6cm] (fbank) at (3.2, 1.8) {Fbank};
    \node[box, minimum width=2.8cm] (enc1) at (6.8, 1.8) {Conformer/Zipformer};
    \node[featbox, minimum width=1.6cm] (feat1) at (10.6, 1.8) {连续特征};
    \draw[arr] (wave.east) -- ++(0.5,0) |- (fbank.west);
    \draw[arr] (fbank) -- (enc1);
    \draw[arr] (enc1) -- (feat1);
    \node[lbl, anchor=south] at (6.8, 2.5) {传统端到端训练};

    % 路径2：自监督
    \node[box, minimum width=3.8cm] (ssl) at (5.0, 0) {wav2vec 2.0 / HuBERT / WavLM};
    \node[featbox, minimum width=1.6cm] (feat2) at (10.6, 0) {连续特征};
    \draw[arr] (wave) -- (ssl);
    \draw[arr] (ssl) -- (feat2);
    \node[lbl, anchor=south] at (5.0, 0.7) {自监督学习};

    % 路径3：监督
    \node[box, minimum width=3.8cm] (sup) at (5.0, -1.8) {Whisper / OWSM / SenseVoice};
    \node[featbox, minimum width=1.6cm] (feat3) at (10.6, -1.8) {连续特征};
    \draw[arr] (wave.east) -- ++(0.5,0) |- (sup.west);
    \draw[arr] (sup) -- (feat3);
    \node[lbl, anchor=south] at (5.0, -1.1) {监督学习};

    \end{tikzpicture}
    \bicaption{\enspace 用于理解的语音表征分类：按训练范式分为传统端到端训练、自监督学习和监督学习三类}{\enspace Taxonomy of speech representations for understanding: traditional end-to-end training, self-supervised learning, and supervised learning}
    \label{fig:rw_repr_understand}
\end{figure}
