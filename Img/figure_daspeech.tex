\begin{figure}[!htbp]
    \centering
    \includegraphics[width=\textwidth]{Img/DASpeech.pdf}
    \bicaption{\enspace DASpeech模型架构示意图。语言解码器最后一层的隐状态被组织为有向无环图。训练时，声学解码器的输入为期望隐状态序列；推理时，声学解码器的输入为概率最大路径上的隐状态序列。}{\enspace Overview of DASpeech. The last-layer hidden states of the linguistic decoder are organized as a DAG. During training, the input to the acoustic decoder is the sequence of expected hidden states. During inference, it is the sequence of hidden states on the most probable path.}
    \label{fig:daspeech}
\end{figure}
